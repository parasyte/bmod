[package]
name = "test-plugin"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
bmod = { path = "../.." }
cxx = "1"

[build-dependencies]
bmod-build = { path = "../../bmod-build" }
